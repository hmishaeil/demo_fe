import { EmailVerification } from './email-verification.model';

describe('EmailVerification', () => {
  it('should create an instance', () => {
    expect(new EmailVerification()).toBeTruthy();
  });
});
