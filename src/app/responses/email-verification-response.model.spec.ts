import { EmailVerificationResponse } from './email-verification-response.model';

describe('EmailVerificationResponse', () => {
  it('should create an instance', () => {
    expect(new EmailVerificationResponse()).toBeTruthy();
  });
});
