import { InitResetPasswordRequest } from './init-reset-password-request.model';

describe('InitResetPasswordRequest', () => {
  it('should create an instance', () => {
    expect(new InitResetPasswordRequest()).toBeTruthy();
  });
});
