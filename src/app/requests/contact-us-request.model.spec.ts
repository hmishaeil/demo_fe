import { ContactUsRequest } from './contact-us-request.model';

describe('ContactUsRequest', () => {
  it('should create an instance', () => {
    expect(new ContactUsRequest()).toBeTruthy();
  });
});
